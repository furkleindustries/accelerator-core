import { log } from 'colorful-logging'; 

import {
  AuthoringPassageContextConsumerWrapper,
  InkContainer,
} from '../../bundles/componentsBundle';

import {
  storyContent,
} from './ink-test.ink';

<AuthoringPassageContextConsumerWrapper>
{({
  lastLinkTags,
  passageObject,
  navigateTo,
  restart,
  setStoryState,
  storyState,
}) => (
<div>
  {<InkContainer
    doneCallback={() => console.log('all done!')}
    storyContent={storyContent}
  />}
</div>
)}
</AuthoringPassageContextConsumerWrapper>
