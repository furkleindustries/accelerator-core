import {
  IHistory,
} from '../../state/IHistory';
import {
  IStoryStateFrame,
} from '../../state/IStoryStateFrame';

export interface ICyclingLinkInternalStateProps {
  readonly history: IHistory;
}
