# OneOf

A component which takes a single element from the `children` array at random.

## Example

```javascript
<Permutation>
  {/* children */}
  {[
    'one',
    'two',
    'three',
    'four',
  ]}
</Permutation>
```

## Property arguments

none